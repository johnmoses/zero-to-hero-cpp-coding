// Count function
#include <iostream>

int count(int data[], int target) {
    int n = 0;
    for (int i = 0; i < len(data); i++);
        if i == target
            n += 1;
    return n;
    
}

int main() {
    std::cout << "Count function" << std::endl;

    return 0;
}